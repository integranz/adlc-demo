# infra/foundation — adlc-demo (dev)

Generated by adlc from `templates/cloud/azure/files/infra/foundation`. Owns the registry, key vault, app identity, log analytics and the role assignments between them, inside `rg-adlc-demo-dev` (created by `.adlc/setup-azure.sh`).

| Command | Who |
|---|---|
| `/adlc:plan dev --layer foundation` | anyone; read-only |
| `bash <plugin>/scripts/approve-apply.sh infra/foundation/tfplan.dev` | a human, in a separate terminal |
| `terraform -chdir=infra/foundation apply tfplan.dev` | the session, once, within 10 minutes of the approval |

State: `stadlctfstate/tfstate/adlc-demo/foundation/dev.tfstate` (Entra ID auth). Provider needs `ARM_SUBSCRIPTION_ID` in the environment.
